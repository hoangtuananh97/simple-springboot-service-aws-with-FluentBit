Name: Workflow_a93a
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: PUSH
    Branches:
      - testing1

# Required - Define action configurations.
Actions:
  Build_01:
    # Identifies the action. Do not modify this value.
    Identifier: aws/build@v1.0.0
    # Specifies the source and/or artifacts to pass to the action as input.
    Inputs:
      # Optional
      Sources:
        - WorkflowSource # This specifies that the action requires this Workflow as a source
    Outputs:
      # Optional; Automatically discover reports for popular test frameworks
      AutoDiscoverReports:
        Enabled: false
        # Use as prefix for the report files
        ReportNamePrefix: rpt
      Artifacts:
        - Name: cdkout
          Files:
            - "**/*"
    # Defines the action's properties.
    Configuration:
      # Required - Steps are sequential instructions that run shell commands
      Steps:
        - Run: echo "Hello, World!"
        - Run: echo "<?xml version=\"1.0\" encoding=\"UTF-8\" ?>" >> report.xml
        - Run: echo "<testsuite tests=\"1\" name=\"TestAgentJunit\" >" >> report.xml
        - Run: echo "<testcase classname=\"TestAgentJunit\" name=\"Dummy
            Test\"/></testsuite>" >> report.xml
      Container:
        Registry: CODECATALYST
        Image: CodeCatalystLinux_x86_64:2024_03
    Compute:
      Type: EC2
      Fleet: Linux.x86-64.Large
    Environment:
      Name: testing-enviromment
  AWSCDKDeploy:
    # Identifies the action. Do not modify this value.
    Identifier: aws/cdk-deploy@v2.0.2
    # Required; You can use an environment to access AWS resources.
    Environment:
      Name: testing-enviromment
    # Specifies the source and/or artifacts to pass to the action as input.
    Inputs:
      # Optional
      Sources:
        - WorkflowSource # This specifies that the action requires this Workflow as a source
      Artifacts:
        - cdkout
    Compute:
      Type: EC2 # Defines the action's properties.
    Configuration:
      StackName: SpringbootFargateCdkStack
      CdkRootPath: springboot-application-aws-infra/
      Region: ap-southeast-1
      CdkCliVersion: 2.179.0
    DependsOn:
      - AWSCDKBootstrap
  AWSCDKBootstrap:
    # Identifies the action. Do not modify this value.
    Identifier: aws/cdk-bootstrap@v2.0.1
    # Required; You can use an environment to access AWS resources.
    Environment:
      Name: testing-enviromment
    Inputs:
      Artifacts:
        - cdkout
    Compute:
      Type: EC2 # Defines the action's properties.
    Configuration:
      Region: ap-southeast-1
      CdkCliVersion: 2.179.0
    DependsOn:
      - Build_01
